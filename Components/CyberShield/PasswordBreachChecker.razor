<!-- PasswordBreachChecker.razor — Password input → SHA-1 hash → HIBP k-Anon → result -->

@code {
    // TODO: Password input field
    // TODO: Client-side SHA-1 hash → send only 5-char prefix to HIBP API
    // TODO: Zero password data leaves browser — no API key required
}
